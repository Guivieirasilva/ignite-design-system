{
  "name": "first-ignite-ui",
  "version": "1.0.0",
  "private": "true",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=!docs && changeset publish"
  },
  "packageManager": "npm@9.6.6",
  "devDependencies": {
    "@changesets/cli": "^2.27.9",
    "@storybook/addon-a11y": "^7.6.20",
    "turbo": "^2.1.3"
  }
}
